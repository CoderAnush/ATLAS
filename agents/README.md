# AI Agents

Specialized agents owning ML lifecycle stages. All I/O must use versioned contracts from `packages/atlas-contracts`.

Orchestration is owned by `agents/orchestrator`.

See `idea.md` § AI Agents.

# ADR 0000 — Record Architecture Decisions

## Status

Accepted

## Context

ATLAS is a long-lived enterprise platform. Decisions must be traceable.

## Decision

We will store Architecture Decision Records under `docs/adr/`. Locked product decisions also appear in `idea.md`.

## Consequences

PRs that change architecture should add/update an ADR and sync `idea.md`.
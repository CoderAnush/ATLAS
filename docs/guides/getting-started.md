# Getting Started

Foundation only is present. After Phase 1:

1. Install `uv` and `pnpm`
2. Copy `.env.example` → `.env`
3. `docker compose up`
4. Open the web UI and API docs

Until then, read `idea.md`, `ARCHITECTURE.md`, and `ROADMAP.md`.
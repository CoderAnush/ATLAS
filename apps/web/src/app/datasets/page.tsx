import { PlaceholderPage } from "@/components/PlaceholderPage";

export default function DatasetsPage() {
  return (
    <PlaceholderPage
      title="Datasets"
      description="Dataset ingestion and versioning land in Phase 3. Storage backends are already wired in the platform foundation."
    />
  );
}

import { PlaceholderPage } from "@/components/PlaceholderPage";

export default function SettingsPage() {
  return (
    <PlaceholderPage
      title="Settings"
      description="Tenant preferences, API keys, and integrations will live here after authentication lands in Phase 2."
    />
  );
}

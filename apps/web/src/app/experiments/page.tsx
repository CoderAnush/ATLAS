import { PlaceholderPage } from "@/components/PlaceholderPage";

export default function ExperimentsPage() {
  return (
    <PlaceholderPage
      title="Experiments"
      description="Experiment tracking will connect to MLflow in later phases. The tracking server is available in Compose today."
    />
  );
}

import { PlaceholderPage } from "@/components/PlaceholderPage";

export default function MonitoringPage() {
  return (
    <PlaceholderPage
      title="Monitoring"
      description="Prometheus and Grafana are part of the Phase 1 stack. Product monitoring views will build on those signals."
    />
  );
}

import { PlaceholderPage } from "@/components/PlaceholderPage";

export default function ModelsPage() {
  return (
    <PlaceholderPage
      title="Models"
      description="Model registry and approvals are scheduled after training. This shell keeps navigation ready."
    />
  );
}

[project]
name = "atlas-core"
version = "0.1.0"
description = "Shared core primitives for ATLAS."
readme = "README.md"
requires-python = ">=3.11"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/atlas_core"]
